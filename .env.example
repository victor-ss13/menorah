# Configurações da aplicação
# Copie este arquivo para .env e preencha os valores

# --- API ---
# URL base da API externa (ex: https://api.example.com)
API_BASE_URL=https://api.example.com
# Timeout em segundos para requisições HTTP
API_TIMEOUT=30

# --- Diretórios ---
# Diretório de entrada com planilhas a processar
INPUT_DIR=data/input
# Diretório de saída para planilhas geradas
OUTPUT_DIR=data/output

# --- Logging ---
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Caminho do arquivo de log
LOG_FILE=logs/excel_automation.log
